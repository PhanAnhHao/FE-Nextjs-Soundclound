const PlaylistPage = () => {
    console.log("Check ENV BACKEND_URL at playlist page:", process.env.NEXT_PUBLIC_BACKEND_URL);
    return (
        <div>PlaylistPage</div>
    );
};

export default PlaylistPage;